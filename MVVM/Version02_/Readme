1.version01中，虽然主要功能实现了。但是存在几个问题：
    1).当咱们把UI中的控件类型变了，比如把按钮变为拖动的控件，也就是说数字可以单手调控。那么当咱们控件变了后背后的业务逻辑就得跟着变，这就不行。比如原来的输入类型转换、输入类型判别...
       界面一动业务逻辑代码就得跟着动，界面一动业务逻辑代码就得跟着动，...这个（当程序很大时）开销很大的，维护成本也很高
       咱们的version02，先把之前的事件处理程序删了，因为，UI变了嘛，咱们先自己写写。